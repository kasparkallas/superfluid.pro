# Fetch all tokens -- both Super Tokens and underlying tokens
query FetchAllTokens($id_gt: ID!) {
  tokens(first: 1000, where: {id_gt: $id_gt}) {
    id
    name
    decimals
    symbol
    underlyingAddress
    isListed
    isNativeAssetSuperToken
    isSuperToken
  }
}