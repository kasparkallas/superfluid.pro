{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".next/**"],
			"env": ["PAYLOAD_SECRET", "DATABASE_URL"]
		},
		"dev": {
			"persistent": true,
			"cache": false,
			"env": ["PORT"]
		},
		"typecheck": {
			"dependsOn": ["^build"],
			"inputs": ["**/*.ts", "**/*.tsx", "**/tsconfig.json"]
		}
	}
}
